/* Crie uma função que dado dois valores (passados como parâmetros) mostre no console a soma, subtração,
multiplicação e divisão desses valores.
*/ 
const mult = function(A,B){
    var  total = (A*B)
    return total

}

 var A = 15
 var B = 25
console.log(`esse é o valor da multiplicação de A e B ${mult (A, B)}`)




const division = function(C,D) {
    var total = (C/D)
    return total
}

var C = 100
var D = 10
console.log(`esse  é o valor da divisão de C e D ${division(C,D)}`)