let precodoBone = 200;
let desconto = 0.15;
let precocomDesconto = precodoBone * (1 - desconto);
console.log(precocomDesconto)

let nome = "Bone";
let propriedade = "New Era"; 
console.log("Nome: "  + nome + ", Propriedade: " + propriedade + ", Preço: " 
+precodoBone);